@using Vidly.Models
@model Customer

@{
    ViewBag.Title = "Details";
}

<h2>@Model.Name</h2>

<p>Membership Type: @Model.MembershipType.Name</p>
@{
    if (Model.BirthDate.HasValue) {
    <p>Birth Date: @Model.BirthDate.Value.ToShortDateString()</p>
    }
}